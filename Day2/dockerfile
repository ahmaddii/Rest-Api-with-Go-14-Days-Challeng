# Step 1: Use official Go image
FROM golang:1.25-alpine

# Step 2: Set working directory inside container
WORKDIR /app

# Step 3: Copy go.mod and go.sum files
COPY go.mod go.sum ./

# Step 4: Download dependencies
RUN go mod download

# Step 5: Copy all source code
COPY . .

# Step 6: Build the Go application
RUN go build -o main .

# Step 7: Expose the port your API runs on (e.g., 8080)
EXPOSE 9090

# Step 8: Command to run the binary
CMD ["./main"]
